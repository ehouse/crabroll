# crabroll - micro-diceroller

A dice expression language and evaluator written in Rust. Will eventually compile to a CLI tool and a WASM package for embedding in websites.

## Running tests

```bash
cargo test
```
